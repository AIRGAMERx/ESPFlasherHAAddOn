#include <tunables/global>

/usr/bin/python3 {
  #include <abstractions/base>

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  /data/** rwk,
  /tmp/** rw,
  /etc/ssl/** r,

  /usr/bin/** rix,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  /dev/null rw,
  /dev/urandom r,

  deny /mnt/** rwklx,
  deny /media/** rwklx,
  deny /share/** rwklx,
  deny /config/** rwklx,
}
