#include <tunables/global>
profile "io.airgamer.espflasher-web" flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  network inet,
  network inet6,

  /usr/bin/**       rix,
  /usr/lib/**       mr,
  /usr/local/lib/** mr,

  /data/**          rwk,
  /tmp/**           rwk,
  /etc/ssl/**       r,

  /dev/null         rw,
  /dev/urandom      r,
}
